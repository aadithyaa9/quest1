# Runtime dependencies for Dialogue Frame Detector v2.
#
# Requires Python 3.11+.
# FFmpeg and ffprobe are system dependencies and must be installed separately;
# they cannot be installed through pip.

# Local frame extraction and audio transcription
faster-whisper>=1.0.0
opencv-python>=4.9.0

# Video metadata/subtitle retrieval & parsing
requests
yt-dlp>=2024.1.1

# Numerical array manipulation for in-memory audio byte streaming
numpy>=1.26.0

# High-performance string matching for dialogue candidate verification
rapidfuzz>=3.6.1